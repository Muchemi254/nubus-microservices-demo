# k8s/secrets.yaml
apiVersion: v1
kind: Secret
meta
  name: nubus-demo-secrets
  namespace: nubus-demo
type: Opaque

  # In production, use sealed secrets or external secret management
  # Example: echo -n "password" | base64
  db-password: cGFzc3dvcmQ=  # "password" base64 encoded